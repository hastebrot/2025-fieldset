import { cva, type VariantProps } from "class-variance-authority";
import { clsx, type ClassValue } from "clsx";

export {
  // https://github.com/lukeed/clsx
  clsx as classNames,
  // https://github.com/joe-bell/cva
  cva as classVariants,
  type ClassValue,
  type VariantProps,
};
